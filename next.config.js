
/*const nextConfig = {
  output: 'export',
  
}

module.exports = nextConfig*/
/** @type {import('next').NextConfig} */
module.exports = {
  output: 'export',
  experimental: {
    images: true,
  },
  // Other configurations...
};

